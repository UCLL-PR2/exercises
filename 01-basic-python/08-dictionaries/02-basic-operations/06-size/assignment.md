# Assignment

Write a Python function `size(dictionary)` that returns the number of key/value pairs in `dictionary`.

For example,

```python
>>> size({})
0

>>> size({'a': 1})
1

>>> size({'a': 1, 'b': 2})
2
```

# Assignment

Write a Python function `keys(dictionary)` that returns all keys as a list.
The order is unimportant.

For example,

```python
>>> keys({})
[]

>>> keys({'a': 1})
['a']

>>> keys({'a': 1, 'b': 2})
['a', 'b']
```
